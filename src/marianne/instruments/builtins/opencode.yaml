# OpenCode — Headless AI coding agent with OpenRouter support
#
# OpenCode (opencode.ai) is a CLI coding agent supporting 75+ LLM providers
# including OpenRouter natively. Uses -p flag for non-interactive prompt
# execution. Supports MCP servers via mcpServers config.
#
# Note: The OpenCode repo has been archived; development continues as "Crush"
# by Charm team. The deep fallback chain ensures continuity if OpenCode
# becomes unavailable.
#
# Auth: Configured via `opencode providers login`. Supports OpenRouter,
#       Z.AI, and other providers. Free-tier models available without credits.
#
# Default provider: OpenRouter (free-tier models)
# Headless mode: -p flag for non-interactive, -f json for structured output

name: opencode
display_name: "OpenCode"
description: "Headless AI coding agent with OpenRouter and MCP support"
kind: cli

capabilities:
  - tool_use
  - file_editing
  - shell_access
  - mcp
  - structured_output

# Free-tier OpenRouter models — all $0 input/output
default_model: "qwen/qwen3-coder:free"
default_timeout_seconds: 1800

models:
  # ═══════════════════════════════════════════════════════════════════
  # Coding — models optimized for code generation, TDD, refactoring
  # ═══════════════════════════════════════════════════════════════════
  - name: "qwen/qwen3-coder:free"
    context_window: 262000
    cost_per_1k_input: 0.0
    cost_per_1k_output: 0.0
    max_output_tokens: 262000
    max_concurrent: 8
  - name: "openai/gpt-oss-120b:free"
    context_window: 131072
    cost_per_1k_input: 0.0
    cost_per_1k_output: 0.0
    max_output_tokens: 131072
    max_concurrent: 8
  - name: "openai/gpt-oss-20b:free"
    context_window: 131072
    cost_per_1k_input: 0.0
    cost_per_1k_output: 0.0
    max_output_tokens: 8192
    max_concurrent: 8

  # ═══════════════════════════════════════════════════════════════════
  # Reasoning — deep thinking, architecture, complex multi-step work
  # ═══════════════════════════════════════════════════════════════════
  - name: "nousresearch/hermes-3-llama-3.1-405b:free"
    context_window: 131072
    cost_per_1k_input: 0.0
    cost_per_1k_output: 0.0
    max_output_tokens: 131072
    max_concurrent: 8
  - name: "nvidia/nemotron-3-super-120b-a12b:free"
    context_window: 262144
    cost_per_1k_input: 0.0
    cost_per_1k_output: 0.0
    max_output_tokens: 262144
    max_concurrent: 8
  - name: "qwen/qwen3-next-80b-a3b-instruct:free"
    context_window: 131072
    cost_per_1k_input: 0.0
    cost_per_1k_output: 0.0
    max_output_tokens: 65536
    max_concurrent: 8
  - name: "moonshotai/kimi-k2:free"
    context_window: 131072
    cost_per_1k_input: 0.0
    cost_per_1k_output: 0.0
    max_output_tokens: 65536
    max_concurrent: 8

  # ═══════════════════════════════════════════════════════════════════
  # General-purpose — balanced capability for building, research, writing
  # ═══════════════════════════════════════════════════════════════════
  - name: "google/gemma-4-31b-it:free"
    context_window: 262144
    cost_per_1k_input: 0.0
    cost_per_1k_output: 0.0
    max_output_tokens: 32768
    max_concurrent: 8
  - name: "google/gemma-4-26b-a4b-it:free"
    context_window: 262144
    cost_per_1k_input: 0.0
    cost_per_1k_output: 0.0
    max_output_tokens: 32768
    max_concurrent: 8
  - name: "meta-llama/llama-3.3-70b-instruct:free"
    context_window: 65536
    cost_per_1k_input: 0.0
    cost_per_1k_output: 0.0
    max_output_tokens: 65536
    max_concurrent: 8
  - name: "minimax/minimax-m2.5:free"
    context_window: 196608
    cost_per_1k_input: 0.0
    cost_per_1k_output: 0.0
    max_output_tokens: 8192
    max_concurrent: 8
  - name: "z-ai/glm-4.5-air:free"
    context_window: 131072
    cost_per_1k_input: 0.0
    cost_per_1k_output: 0.0
    max_output_tokens: 96000
    max_concurrent: 8
  - name: "arcee-ai/trinity-large-preview:free"
    context_window: 131000
    cost_per_1k_input: 0.0
    cost_per_1k_output: 0.0
    max_output_tokens: 131000
    max_concurrent: 8

  # ═══════════════════════════════════════════════════════════════════
  # Fast & light — quick tasks, validation checks, simple transforms
  # ═══════════════════════════════════════════════════════════════════
  - name: "nvidia/nemotron-3-nano-30b-a3b:free"
    context_window: 256000
    cost_per_1k_input: 0.0
    cost_per_1k_output: 0.0
    max_output_tokens: 256000
    max_concurrent: 8
  - name: "nvidia/nemotron-nano-9b-v2:free"
    context_window: 131072
    cost_per_1k_input: 0.0
    cost_per_1k_output: 0.0
    max_output_tokens: 32768
    max_concurrent: 8
  - name: "google/gemma-3n-e4b-it:free"
    context_window: 32768
    cost_per_1k_input: 0.0
    cost_per_1k_output: 0.0
    max_output_tokens: 8192
    max_concurrent: 8
  - name: "qwen/qwen3-4b:free"
    context_window: 131072
    cost_per_1k_input: 0.0
    cost_per_1k_output: 0.0
    max_output_tokens: 32768
    max_concurrent: 8
  - name: "liquid/lfm-2.5-1.2b-thinking:free"
    context_window: 32768
    cost_per_1k_input: 0.0
    cost_per_1k_output: 0.0
    max_output_tokens: 8192
    max_concurrent: 8

cli:
  command:
    executable: opencode
    subcommand: "run"
    # Message is positional (no flag). -p is password, NOT prompt.
    prompt_via_stdin: false
    model_flag: "--model"
    output_format_flag: "--format"
    output_format_value: "json"
    # Auth handled by opencode's credential store (opencode providers login).
    # Run `opencode providers login` to configure providers.
    # Keys stored in ~/.local/share/opencode/auth.json
  output:
    format: json
    result_path: "result"
    error_path: "error"
    input_tokens_path: "usage.input_tokens"
    output_tokens_path: "usage.output_tokens"
    aggregate_tokens: false
  errors:
    success_exit_codes: [0]
    rate_limit_patterns:
      - "rate.?limit"
      - "(?:error|status|code)[^\\w]*429"
      - "too many requests"
    auth_error_patterns:
      - "unauthorized"
      - "invalid.*api.*key"
      - "authentication.*failed"
    capacity_patterns:
      - "overloaded"
      - "503"
      - "capacity"
    timeout_patterns:
      - "timed?.?out"
      - "timeout"
