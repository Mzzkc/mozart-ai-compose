{# Sheet 1: RECON — survey project state and shared context #}
# Cycle Reconnaissance

You are {{ agent_name | title }}. Survey the current state of the project
and your own situation. Produce a reconnaissance report.

## Shared Context Map

Review the shared directory listing (loaded in prelude) for an overview of
what exists. Do not read everything — scan for relevance to your focus
({{ focus }}).

{% if shared_listing is defined and shared_listing %}
The shared directory structure is available in your prelude context.
Identify which subdirectories contain artifacts relevant to this cycle.
{% endif %}

## Read These

1. Your recent activity (loaded in context)
2. Task registry — `{{ workspace }}/collective/tasks.md`
3. Composer directives — `{{ workspace }}/shared/directives/`
4. Collective memory — `{{ workspace }}/collective/memory.md`
5. Other agents' recent reports — `{{ workspace }}/agents/*/reports/`
6. Recent git log — `git log --oneline -20`

## Shared Space Curation

Check `{{ workspace }}/shared/active/` for current coordination artifacts.
If stale or irrelevant items exist, archive them to `{{ workspace }}/shared/archive/`.
If you find specs or plans relevant to your focus, copy them to active.

{% if technique_manifest is defined and technique_manifest %}
{{ technique_manifest }}
{% endif %}

## What to Capture

Write to `{{ workspace }}/agents/{{ agent_name }}/cycle-state/recon.md`:

### What Changed
What happened since last cycle? New commits, findings, tasks, directives.

### Your Opportunities
Unclaimed tasks matching your focus ({{ focus }})? List with priorities.

### Mateship Signals
Blocked agents? Uncommitted work? Stale findings? Note who needs help.

### Composer Directives
Any notes in `shared/directives/` for you or the team?

### Coordination State
What is in `shared/active/`? Is it current? What should be there?

**Output:** `{{ workspace }}/agents/{{ agent_name }}/cycle-state/recon.md`
