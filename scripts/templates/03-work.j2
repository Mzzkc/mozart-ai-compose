{# Sheet 3: WORK — execute against plan with TDD #}
# Cycle Work

## Commander's Intent

**PURPOSE:** Execute your cycle plan. Build toward the project's goals through {{ focus }}.

**END STATE:** Tasks completed, tested, committed. Tests pass. Evidence exists.

**CONSTRAINTS:**
- Do not work on unclaimed tasks
- Do not modify files another agent is working on (check shared plans)
- Do not skip tests — write the test first, then implement
- Stage only your files: `git add <specific files>` — never `git add .`
- Commit with: `git commit -m "{{ agent_name }}: <what you did>"`
{% if pre_commit_commands %}
- Before committing, run:
{% for cmd in pre_commit_commands %}
  - `{{ cmd }}`
{% endfor %}
{% endif %}

**FREEDOMS:** Choose your approach. Refactor adjacent code if it blocks you. File findings. Add tasks.

{% if technique_manifest is defined and technique_manifest %}
{{ technique_manifest }}
{% endif %}

## Your Plan

Read: `{{ workspace }}/agents/{{ agent_name }}/cycle-state/plan.md`

## Execute

For each task:
1. Read relevant code and specs
2. Write a failing test
3. Implement minimum to pass
4. Run validations:
{% if validations %}
{% for v in validations %}
   - `{{ v.command }}`{% if v.description %} — {{ v.description }}{% endif %}

{% endfor %}
{% endif %}
5. Commit with evidence
6. Update task registry: `{{ workspace }}/collective/tasks.md`

## Mateship

If you find something another agent should know:
- File findings to `{{ workspace }}/shared/findings/` with severity (P0/P1/P2)
- Pick up unowned findings in your focus area

Memory: `{{ agent_identity_dir }}/recent.md`

**Output:** Committed code. Updated tasks, findings if applicable.
