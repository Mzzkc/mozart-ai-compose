# Marianne AI Compose — Status

Do not trust this file blindly. Run `pytest tests/ -x`, `mypy src/`, and `ruff check src/` to verify current state. Check `git log --oneline -20` for recent work.

## Current Phase

v1 Beta — Baton is the default execution model. Orchestra-driven development (v1-beta-v3 score).

## Known Issues

Check `gh issue list --repo Mzzkc/marianne-ai-compose` for current bugs and priorities.

## Architecture

See `.marianne/spec/` for the specification corpus. See `CLAUDE.md` for development guidance.
